
#相同点：
go install与go build都可生成执行文件

#区别：
## go build：

- go build 会在当前目录中生成可实行文件
- go build 用于编译包生成可执行文件，必须有main包才可以
- 在包的编译过程中，若有必要，会同时编译与之相关联的包
- 名字
    - 默认情况下这个文件的名字为源文件名字去掉.go后缀
    - 可以通过-o选项来指定其他名字
    - 直接执行go build命令，后面不带文件名，我们将得到一个与目录名同名的可执行文件


## go install：
所以他是依赖GOPATH的
- 两步操作：第一步是生成结果文件（可执行文件或者 .a 包），第二步会把编译好的结果移到 $GOPATH/pkg 或者 $GOPATH/bin。
- 将编译的中间文件放在 GOPATH 的 pkg 目录下，以及固定地将编译结果放在 GOPATH 的 bin 目录下